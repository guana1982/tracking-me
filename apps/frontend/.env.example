# API URL - leave empty for local development (uses Vite proxy)
# In production, set this to your Railway backend URL (without /api suffix)
# Example: https://your-backend.up.railway.app
VITE_API_URL=
