[build]
builder = "nixpacks"
installCommand = "npm install -g pnpm && cd ../.. && pnpm install"
buildCommand = "cd ../.. && pnpm --filter @budget/shared build && pnpm --filter @budget/frontend build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"

[variables]
NODE_ENV = "production"
