[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @budget/shared build && pnpm --filter @budget/frontend build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"

[variables]
NODE_ENV = "production"
