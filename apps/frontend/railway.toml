[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && cd ../.. && pnpm install && pnpm --filter @budget/shared build && pnpm --filter @budget/frontend build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"

[variables]
NODE_ENV = "production"
